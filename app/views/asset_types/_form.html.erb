<%= form_with(model: asset_type) do |form| %>
  <% if asset_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(asset_type.errors.count, "error") %> prohibited this asset_type from being saved:</h2>

      <ul>
        <% asset_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nombre, style: "display: block" %>
    <%= form.text_field :nombre %>
  </div>

  <div>
    <%= form.label :observaciones, style: "display: block" %>
    <%= form.text_area :observaciones %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
